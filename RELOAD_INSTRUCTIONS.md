# 重新加载插件指南

## 问题症状

插件列表中可以看到 **Journals** 插件，但：
- ❌ 没有插件管理页面链接
- ❌ 只能看到设置选项
- ❌ 无法访问导入界面

## 原因

客户端国际化文件缺少 `title` 键，导致插件路由注册失败。

## 已修复

✅ 添加了 `discourse_journals.title` 翻译键（英文和中文）

## 重新加载步骤

### 方法1：开发环境（推荐）

如果你在本地开发环境（localhost）：

```bash
# 进入 Discourse 目录
cd /Users/youngp/discourse

# 重启 Rails 服务器
# 按 Ctrl+C 停止当前服务器，然后重新运行：
bin/ember-cli -u

# 或者如果使用 docker:
docker-compose restart
```

### 方法2：生产环境

如果在生产服务器：

```bash
# SSH 登录服务器
ssh user@your-server

# 进入 Discourse 目录
cd /var/www/discourse

# 重新编译前端资源
RAILS_ENV=production bin/rake assets:precompile

# 重启 Unicorn
sv restart unicorn

# 重启 Sidekiq（如果需要）
sv restart sidekiq
```

### 方法3：Docker 环境

```bash
cd /var/discourse
./launcher rebuild app
```

## 验证步骤

重启后，按以下步骤验证：

1. **清除浏览器缓存**
   - Chrome/Edge: Ctrl+Shift+Del
   - Firefox: Ctrl+Shift+Del
   - Safari: Cmd+Option+E

2. **刷新页面**
   - 硬刷新: Ctrl+Shift+R (Windows/Linux) 或 Cmd+Shift+R (Mac)

3. **检查插件列表**
   - 进入：`Admin → Plugins → 安装的插件`
   - 找到 **Journals** 插件
   - 应该能看到插件名称可以点击

4. **访问管理页面**
   - 点击 **Journals** 插件名称
   - 应该看到导入界面，包含：
     - ✅ "Import Journals" / "导入期刊" 标题
     - ✅ 文件上传说明
     - ✅ JSON 文件选择器
     - ✅ "Start import" / "开始导入" 按钮

5. **或直接访问 URL**
   ```
   http://localhost:4200/admin/plugins/journals
   ```

## 预期界面

### 中文界面
```
┌────────────────────────────────────────┐
│ 期刊库                                  │
├────────────────────────────────────────┤
│                                        │
│ 导入期刊                                │
│                                        │
│ 上传包含期刊数据的 JSON 文件...          │
│                                        │
│ JSON 文件 (.json)                      │
│ [选择文件...]                           │
│                                        │
│ [开始导入]                              │
│                                        │
└────────────────────────────────────────┘
```

### 英文界面
```
┌────────────────────────────────────────┐
│ Journals                               │
├────────────────────────────────────────┤
│                                        │
│ Import Journals                        │
│                                        │
│ Upload a JSON file containing...      │
│                                        │
│ JSON file (.json)                      │
│ [Choose file...]                       │
│                                        │
│ [Start import]                         │
│                                        │
└────────────────────────────────────────┘
```

## 故障排除

### 1. 重启后仍然看不到管理页面

**检查文件是否存在**：
```bash
ls -la plugins/discourse-journals/config/locales/client.en.yml
cat plugins/discourse-journals/config/locales/client.en.yml | grep "title:"
```

应该看到：
```yaml
discourse_journals:
  title: "Journals"
```

### 2. 浏览器控制台错误

按 F12 打开开发者工具，检查 Console 标签：

**常见错误**：
- `Cannot find module` → 重新编译资源
- `Route not found` → 检查路由配置
- `Translation missing` → 检查国际化文件

### 3. 前端资源未更新

强制重新编译：
```bash
# 开发环境
rm -rf tmp/cache
bin/ember-cli -u

# 生产环境
rm -rf public/assets
RAILS_ENV=production bin/rake assets:precompile
```

### 4. 插件未正确加载

检查插件状态：
```bash
# 进入 Rails 控制台
RAILS_ENV=production bin/rails c

# 检查插件是否加载
Discourse.plugins.map(&:name)
# 应该包含 "discourse-journals"
```

## 开发环境快速测试

如果你在本地开发：

1. **停止服务器** (Ctrl+C)

2. **清理缓存**
   ```bash
   rm -rf tmp/cache
   ```

3. **重启服务器**
   ```bash
   bin/ember-cli -u
   ```

4. **在新的浏览器隐身窗口测试**
   - Chrome: Ctrl+Shift+N
   - Firefox: Ctrl+Shift+P

5. **直接访问**
   ```
   http://localhost:4200/admin/plugins/journals
   ```

## 成功标志

✅ 插件列表中 **Journals** 可以点击
✅ 点击后显示导入界面（不是设置页面）
✅ 可以选择 JSON 文件
✅ 有"开始导入"按钮
✅ URL 是 `/admin/plugins/journals`（不是 `/admin/plugins/journals/settings`）

## 下一步

重新加载成功后：

1. 📤 上传 `1.json` 文件测试导入
2. 📊 查看期刊分类中创建的话题
3. 🔄 尝试重复导入（应该更新现有话题）

---

**提示**：如果在生产环境，建议先在测试环境验证，再执行 rebuild。
